## Parent

Part of #9

## What to build

Move Hard cases → detector training images out of the Review IDs right-hand column into a popup opened from a button. Extract behavior is unchanged: same ranges, skip, output directory, and progress. The right column keeps switch markers, names/roles, and save, and has room for the later proposals list.

## Acceptance criteria

- [ ] Hard-case extract controls are not in the main Review IDs right column.
- [ ] A button opens a popup/dialog that hosts the same extract flow.
- [ ] Existing hard-case extract tests still pass; sampling and output behavior are unchanged.
- [ ] Switch markers, names/roles, and save remain on the main Review IDs surface.

## Blocked by

None (can start immediately).
