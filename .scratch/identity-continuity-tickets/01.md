## Parent

Part of #9

## What to build

Detect + track assigns identity slots through a new identity-continuity module that still behaves like today’s greedy last-center-of-mass matcher. Characterization tests lock that current behavior. No user-visible change: same IDs, same dummy-center teleport, same extras dropped.

The detector analysis engine becomes a thin adapter. Interactive advanced uses the same live operation. Interactive basic is unchanged (no per-animal identity slots).

## Acceptance criteria

- [ ] Live identity association for Detect + track goes through one identity-continuity interface (per-frame detections + animals per kind + prior slot state → slot assignment).
- [ ] Default live behavior matches today’s greedy nearest last-center-of-mass, including unmatched slots eventually teleporting to a far dummy center, extras dropped, and first-frame binding.
- [ ] Characterization tests cover at least: 1:1 nearest assignment, extra detections dropped, unmatched slot after the current unused timeout, and a two-animal crossing that today’s matcher swaps.
- [ ] A new Detect + track run produces the same identity-slot assignments as before this ticket on a synthetic or recorded detection sequence used in those tests.
- [ ] No Review IDs, proposal, or occlusion-bout behavior is introduced yet.

## Blocked by

None (can start immediately).
