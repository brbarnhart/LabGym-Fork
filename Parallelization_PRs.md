1. Refactor + sequential export default — extract worker fn; UI/train always set out_folder; still 1 worker. ✅
2. Parallel export + workers control + aug progress bar. ✅
3. Train progress callback + metrics UI. ✅
4. Cancel (aug + train) + button wiring. ✅
5. Tests + polish (tooltips, skip-re-augment, edge cases). ✅
